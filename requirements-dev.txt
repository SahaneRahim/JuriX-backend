# JuriX Backend — Dependances de developpement et de test
#
# Ces paquets n'existaient que dans le groupe dev de pyproject.toml, inaccessible
# via `pip install -r requirements.txt` — le chemin utilise par le Dockerfile et
# par la plupart des developpeurs. La suite de tests etait donc inexecutable.
#
# Usage : pip install -r requirements-dev.txt

-r requirements.txt

# ── Framework de test ─────────────────────────────────────────────────────────
# pytest < 9 : pytest-asyncio 0.24 cible la serie 8.x
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<0.25
pytest-cov>=5.0,<7.0
pytest-mock>=3.14,<4.0
pytest-timeout>=2.3,<3.0

# ── Doublures HTTP ────────────────────────────────────────────────────────────
# respx intercepte httpx : necessaire pour tester llama_parse_service sans
# appeler l'API LlamaParse (donc sans payer).
respx>=0.21,<0.23

# ── Qualite ───────────────────────────────────────────────────────────────────
black>=24.0
ruff>=0.6
