# JuriX Backend — Production dependencies
# Generated from pyproject.toml [tool.poetry.dependencies]
# DO NOT use pip freeze — this file is curated manually

# ── Core Framework ────────────────────────────────────────────────────────────
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
# [email] fournit email-validator, requis par EmailStr (app/schemas/user.py,
# app/api/routes/admin.py). Sans lui, l'import de app.main echoue.
pydantic[email]>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.23,<3.0.0
alembic>=1.12.1,<2.0.0
psycopg2-binary>=2.9.9,<3.0.0
asyncpg>=0.29.0,<1.0.0
pgvector>=0.2.3,<1.0.0

# ── Security ──────────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
python-multipart>=0.0.6,<1.0.0

# ── Document Processing ───────────────────────────────────────────────────────
pypdf>=3.17.0,<4.0.0
pdfplumber>=0.10.0,<1.0.0
python-docx>=1.1.0,<2.0.0
pytesseract>=0.3.10,<1.0.0
pdf2image>=1.16.3,<2.0.0

# ── NLP & Language Detection ──────────────────────────────────────────────────
numpy>=1.26.0,<2.0.0
langdetect>=1.0.9,<2.0.0
fasttext-wheel>=0.9.2

# ── ML — Categorisation ───────────────────────────────────────────────────────
scikit-learn>=1.3.2,<2.0.0
joblib>=1.3.2,<2.0.0

# ── LLM — Gemini API ─────────────────────────────────────────────────────────
# SDK google-genai (nouveau). L'ancien google-generativeai n'est plus utilisé :
# gemini_service.py et embedding_service.py importaient deux SDK incompatibles,
# ce qui empechait l'application de demarrer.
google-genai>=1.0.0,<2.0.0

# ── Utils ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0
httpx>=0.25.2,<1.0.0
